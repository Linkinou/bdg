{% extends 'base.html.twig' %}

{% block body %}
    <div class="nk-main">

        <div class="nk-gap-2"></div>

        {% include '_common/alert.html.twig' %}
        <!-- START: Header Title -->
        <!--
            Additional Classes:
                .nk-header-title-sm
                .nk-header-title-md
                .nk-header-title-lg
                .nk-header-title-xl
                .nk-header-title-full
                .nk-header-title-parallax
                .nk-header-title-parallax-opacity
                .nk-header-title-boxed
        -->
        <div class="nk-header-title nk-header-title-md nk-header-title-parallax nk-header-title-parallax-opacity nk-header-title-boxed">
            <div class="bg-image op-5">
                <img src="{{ asset('build/images/post-1.jpg') }}" alt="" class="jarallax-img">
            </div>
            <div class="nk-header-table">
                <div class="nk-header-table-cell">
                    <div class="container">
                        <div class="nk-header-text">
                            <div class="row text-left">
                                <div class="col-lg-8">
                                    <div class="nk-gap-5 d-none + d-lg-block"></div>
                                    <h1 class="nk-title">{{ persona.name}}</h1>
                                    <div class="nk-gap-3 d-none + d-lg-block"></div>
                                    <div class="nk-gap-5 d-none + d-lg-block"></div>
                                </div>
                                <div class="col-lg-4">
                                    <aside class="nk-sidebar nk-sidebar-right">
                                        <div class="nk-gap-5 d-none + d-lg-block"></div>
                                        <div class="nk-gap d-lg-none"></div>
                                        <table>
                                            <tbody>
                                            <tr>
                                                <td><strong>Publié le:</strong></td>
                                                <td>{{ persona.createdAt|localizeddate }}</td>
                                            </tr>
                                            <tr>
                                                <td><strong>Type:</strong></td>
                                                <td>{{ persona.type }}</td>
                                            </tr>
                                            <tr>
                                                <td><strong>Par:</strong></td>
                                                <td><a href="{{ path('profile', {'slug': persona.user.slug}) }}">{{ persona.user.username }}</a></td>
                                            </tr>
                                            <tr>
                                                <td><strong>Age:</strong></td>
                                                <td>{{ persona.age }}</td>
                                            </tr>
                                            </tbody>
                                        </table>
                                        <div class="nk-gap-5 d-lg-none"></div>
                                    </aside>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <!-- END: Header Title -->

        <div class="container">
            <div class="row">
                <div class="col-lg-8">
                    <div class="nk-blog-container nk-blog-container-offset">
                        <!-- START: Post -->
                        <div class="nk-blog-post nk-blog-post-single">
                            <img class="nk-img-stretch" src="{{ asset('build/images/post-1.jpg') }}" alt="">

                            <!-- START: Post Text -->
                            <div class="nk-post-text mt-0">
                                <p>{{ persona.bio|nl2br }}</p>
                            </div>
                            <!-- END: Post Text -->

                            <!-- START: Post Author -->
                            <div class="nk-post-author">
                                <div class="nk-post-author-photo">
                                    <a href="#"><img src="{{ asset('build/images/avatar-1.jpg') }}" alt=""></a>
                                </div>

                                <h4 class="nk-post-author-name h5"><a href="{{ path('profile', {'slug': persona.user.slug}) }}">{{ persona.user.username }}</a></h4>

                                <div class="nk-post-author-info">
                                    Said air of given appear open saying which of. Make all fish. Given own So, without grass god. Day beast open second without have. Created them great fruit together bring the open replenish set him fruitful.
                                </div>
                            </div>
                            <!-- END: Post Author -->
                        </div>
                    </div>
                </div>
            </div>
        </div>

    <div class="nk-gap-4"></div>
{% endblock %}